// This is only run in older version const prompt = require("prompt-sync")();

// Install first: npm install prompt-sync

import promptSync from "prompt-sync";

const prompt = promptSync();

// const users_input = prompt("Enter any value: ");

// console.log(users_input);
